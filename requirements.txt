# ===== Runtime dependencies =====
# LangChain 1.x 稳定线：LangChain 1.x 已弃用 RunnableWithMessageHistory，
# 本项目对话统一采用 LangGraph 状态机实现（app/agent），因此不再需要 langchain 顶层链模块。
langchain>=1.4.0,<2.0.0
langchain-core>=1.6.0,<2.0.0
# LangChain 1.x：被移除的旧 API（如 CacheBackedEmbeddings）由 langchain-classic 提供
langchain-classic>=1.0.0,<2.0.0
langchain-openai>=1.6.0,<2.0.0
langchain-community>=0.4.0,<0.5.0
langgraph>=1.2.0,<1.3.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# ===== RAG dependencies =====
# 文档解析 / 向量库 / 嵌入缓存 / 混合检索（对齐 Java 参考项目的能力）
# langchain-milvus 0.4.x 适配 langchain-core 1.x，配套 pymilvus 3.x
langchain-milvus>=0.4.0,<0.5.0
pymilvus>=3.0.0,<4.0.0
requests>=2.31.0
redis>=5.0.0
rank_bm25>=0.2.2
pypdf>=4.0.0
python-docx>=1.1.0
