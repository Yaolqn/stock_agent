# ===== Runtime dependencies =====
# Pinned to the LangChain 0.3.x stable line for a reproducible environment.
# The code is also compatible with LangChain 1.x - loosen the upper bound to upgrade.
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-openai>=0.3.0,<0.4.0
langgraph>=0.2.0,<1.0.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1
